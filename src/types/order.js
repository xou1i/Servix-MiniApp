/**
 * @typedef {Object} Order
 * @property {string} id
 * @property {string} table
 * @property {string[]} items — جميع الأصناف (مطبخ + بار) للعرض والبحث
 * @property {string[]} kitchenItems
 * @property {string[]} baristaItems
 * @property {number} minutesAgo
 * @property {'pending'|'preparing'|'ready'|'served'|'cancelled'} status
 * @property {string} notes
 */

export {};
