# API base URL — change this to point to your backend
VITE_API_BASE_URL=https://pretty-ends-start.loca.lt/index.html

# Dev Mode: set to "true" to skip real API login (works offline)
# Set to "false" or remove when backend is available
VITE_USE_MOCK_AUTH=false
